# Core runtime dependencies
numpy>=1.24
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.25

# Optional: Qwen Responses API (replaces openai for chat completions)
# httpx is the HTTP client — no openai package needed for Responses API.
# Keep openai for backwards-compatible Chat Completions fallback:
openai>=1.0
